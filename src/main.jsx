import React from "react";
import ReactDOM from "react-dom/client";
import EstadoCuentaApp from "./EstadoCuentaApp";
import "./style.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EstadoCuentaApp />
  </React.StrictMode>
);
